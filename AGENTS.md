# AGENTS.md · 中元特供·关公 写卡工作区

## 你是谁（协作规则）
- 本工作区用于编写 SillyTavern 角色卡「中元特供·关公」，卡型：MVU + Zod（mvu_zod）。
- 你在此只做写卡/工程工作；是否打包、发布由用户确认后另行执行。

## 项目是什么
- 角色卡：关公（中元节主题）。当前阶段：搭写卡框架，卡面内容未定稿。

## 沟通偏好
- 用中文回复；先给结论再展开。
- 改任何运行时相关文件前，先复述要改什么、怎么验证。

## 元规则（每轮都要遵守）
- 源与产物分离：只改 `source/` 下的源；`artifacts/` 是生成的，不手改。
- 字段链同步：改 `zod_schema.js` 必须同步 `InitialVariables.json` 与 `字段账本.md`，反之亦然。
- 进度只写 `进度.md` 一份。
- 模型可见文本（世界书、规则 txt）不含开发注释、事故史、装饰性 Markdown。
- 卡内绝不从 CDN 引入独立 zod 实例，一律用 TavernHelper 注入的全局 `z`。
- 交付前离线验证：JSON/JS 语法、schema↔初始变量对齐、字段链闭环。
- 不写卡面剧情内容，除非用户明确要求进入写卡阶段。

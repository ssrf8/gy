# 字段账本

> 字段链契约：schema → 初始化 → 更新规则 → 投影 → 消费。
> 改 schema 必须同步本账本与 InitialVariables.json（及两份规则 txt 的白名单/示例）。
> 设计权威：source/card/玩法设计.md。

## 顶层容器（stat_data，白名单由 schema 强制）

### 角色（动态角色池，record；预置：凉/惠惠/达克妮丝/阿库娅）
| 路径 | 类型 | 默认 | 写入者 | 读取者 | 生命周期 |
|---|---|---|---|---|---|
| /角色/{id}/名字 | string | 预置已填 | LLM | 状态栏/LLM | 九女白名单预置；池封闭后不再新增 |
| /角色/{id}/在场 | bool | false | LLM | 状态栏/LLM | 进出场景时 replace；状态栏标签栏在场优先、场外置灰 |
| /角色/{id}/状态 | string | '' | LLM | 状态栏/LLM | 姿态/处境，变化时 replace |
| /角色/{id}/心声 | string | '' | LLM | 状态栏/LLM | 内心所想，在场角色每轮可更新 |
| /角色/{id}/名分 | string | '' | LLM | 状态栏/LLM | 收编归属等级（香客/祭品/神婢/神妃/私有物），收编定名分时 replace |
| /角色/{id}/衣着 | string | 预置登场服 descriptions | LLM | 状态栏/LLM | 色情进度刻度尺：当下衣物与暴露状态（何物/齐整或凌乱/破至何处/湿污），随剧情实时 replace；暴露逐级记录（松脱→滑落→撕破→残片→全裸）；收编后体现名分待遇。2026-08-27 写入规范入规则txt④，初值改预置九人登场服 |

### 环境
| 路径 | 类型 | 默认 | 写入者 | 读取者 | 生命周期 |
|---|---|---|---|---|---|
| /环境/日期 | string | '' | LLM | 状态栏/LLM | 每轮 replace |
| /环境/时间 | string | '' | LLM | 状态栏/LLM | 每轮 replace |
| /环境/地点 | string | '' | LLM | 状态栏/LLM | 变化时 replace |
| /环境/氛围 | string | '' | LLM | LLM | 变化时 replace |

## 规则
- 全部字段由 LLM 独占写入，脚本层只读（状态栏渲染只读不写）
- 显灵状态、角色类型、好感度、香火经讨论裁撤（2026-08-22）：神明/凡人形态、信众认知、香火强弱均由叙事层承载，不入库
- 玉佩容器（当前守护灵）、user 容器（状态/位置）、角色.身体情况经讨论裁撤（2026-08-22，状态栏 v3 起不再展示）：当前守护灵的事实源为守护灵世界书条目（常驻蓝灯，开局页翻 enabled），{{user}} 自身状态与角色身体情况由叙事层承载
- 一个字段只有一个写入者：LLM 与脚本不得竞争同一字段
- 数值字段规范：z.coerce.number + clamp + prefault（当前无数值字段，启用时照此）
- **角色池为封闭白名单**（2026-08-27 用户拍板）：仅九名预置女性（凉/惠惠/达克妮丝/阿库娅/辉夜/早坂爱/早坂奈央/藤原千花/藤原萌叶），男角移出；禁止 add 新角色、禁止 remove 成员、物品文书契约概念一律禁入（「原始合伙契约」事件整改）；新登场人物只写叙事不入池
- 角色离场只 replace `在场=false`，不删除记录
- 模型状态对齐依赖世界书「当前变量投影」条目（{{get_message_variable::stat_data}} + 极简字段说明）
- 名分/衣着 2026-08-26 新增（关羽线收编 NSFW 重构）：名分=收编归属等级（由{{user}}定夺，未收编为空）；衣着=穿着/暴露状态

# 字段账本

> 字段链契约：schema → 初始化 → 更新规则 → 投影 → 消费。
> 改 schema 必须同步本账本与 InitialVariables.json。
> 设计权威：source/card/玩法设计.md（当前为定稿字段，写卡阶段按开放点增删）。

## 顶层容器（stat_data，白名单由 schema 强制）

### 玉佩（核心容器）
| 路径 | 类型 | 默认 | 写入者 | 读取者 | 生命周期 |
|---|---|---|---|---|---|
| /玉佩/当前守护灵 | string | '' | LLM | LLM | 未选为空；玩家选定/切换时 replace |
| /玉佩/显灵状态 | string | '未觉醒' | LLM | LLM | 未觉醒 → 苏醒（选定）→ 显灵（关键时刻）|

### 环境
| 路径 | 类型 | 默认 | 写入者 | 读取者 | 生命周期 |
|---|---|---|---|---|---|
| /环境/日期 | string | '' | LLM | LLM | 每轮 replace |
| /环境/时间 | string | '' | LLM | LLM | 每轮 replace |
| /环境/地点 | string | '' | LLM | LLM | 变化时 replace |
| /环境/氛围 | string | '' | LLM | LLM | 变化时 replace |

### user
| 路径 | 类型 | 默认 | 写入者 | 读取者 | 生命周期 |
|---|---|---|---|---|---|
| /user/状态 | string | '' | LLM | LLM | 每轮 replace |
| /user/位置 | string | '' | LLM | LLM | 变化时 replace |

## 规则
- 玉佩字段由 LLM 独占写入，脚本不参与（当前无脚本层）
- 数值字段用 `z.coerce.number` + clamp + prefault，禁止 LLM 写非法类型
- 一个字段只有一个写入者：LLM 与脚本不得竞争同一字段
- 四家信奉不建状态（恒定设定），需要好感度/关系变化时再加 record 容器

# 架构说明 · 中元特供·关公（MVU 写卡框架）

> 版本：v0.1（框架脚手架）｜ 日期：2026-08-20

## 1. 定位
- 卡型：`mvu_zod`（MVU 状态由卡内 Zod schema 约束）
- 能力标志：`tavern_helper`（宿主依赖）、`remote_loader`（CDN 运行时）、`extra_update_model`（双兼容布局）、`embedded_ui`（开局页向导）
- 当前阶段：写卡框架就绪；卡面内容未定稿。

## 2. 三格（本阶段）
- 目标：搭建可写卡的 MVU 框架——组件模板、字段链契约、源/产物目录分离。
- 红线：不发明运行时 API；卡内不引第三方 zod 实例；模型可见文本无开发注释。
- 验收：文件就位、schema↔InitialVariables 对齐、离线语法检查通过；真机验收在写卡阶段执行。

## 3. 文件地图

| 路径 | 角色 | 维护方式 |
|---|---|---|
| `source/components/zod_schema.js` | Zod schema（字段链起点） | 手写 |
| `source/components/InitialVariables.json` | 初始变量（与 schema 对齐） | 手写 |
| `source/components/mvu_update_full.txt` | 更新规则（模型可见） | 手写 |
| `source/components/mvu_update_output_format.txt` | 输出格式（模型可见） | 手写 |
| `source/components/mvu_zod_cn.js` | MVU 加载器（bundle + 默认设置） | 手写 |
| `source/ui/opening-page.html` | 开局页两步选择（选守护灵开关世界书条目 → 选开场白切换首消息，玩家自由输入） | 手写 |
| `source/ui/开局页使用说明.md` | 嵌入 first_mes 步骤、命名约定、正则、真机清单 | 手写 |
| `source/worldbook/世界书布局.md` | 世界书条目源与路由标记 | 手写 |
| `source/worldbook/entries/` | 条目正文源（守护灵×2 + 家族×4，md） | 手写 |
| `source/card/备选开场白.md` | alternate_greetings 正文（当前 1 条先跑通） | 手写 |
| `source/card/first_mes组装.md` | first_mes 拼接说明 + 条目参数表 + 测试路径 | 手写 |
| `source/card/first_mes叙事.txt` | 给模型看的叙事（组装进 first_mes） | 手写 |
| `source/card/openings/` | 备选开场白正文（每个 txt 一条，自动组装进 alternate_greetings） | 手写 |
| `scripts/build-card.mjs` | 组装脚本（只读 source/ → artifacts/） | 手写（开发用） |
| `source/card/卡面占位.md` | 身份/问候/示例对话（后续） | 手写 |
| `字段账本.md` | 字段生命周期契约 | 手写 |
| `架构说明.md` | 本文件 | 手写 |
| `artifacts/` | 生成物（角色卡 JSON、first_mes 等） | 流水线生成 |

## 4. MVU 三层与字段链

- 规则进 prompt：`mvu_update_full.txt` + `mvu_update_output_format.txt`（世界书条目 `[mvu_update]`）
- bundle 执行：`mvu_zod_cn.js` 导入 MagVarUpdate `bundle.js`，解析 `<UpdateVariable>`，执行 JSONPatch
- schema 约束：`zod_schema.js` 经 `registerMvuSchema` 注册，校验 `stat_data`

字段链（每条字段必须能走通）：
`schema → 初始化 → 更新规则 → 模型投影 → 运行时读取 → 写回 → 清理/迁移`

- `stat_data`：注入模型 + HUD 读取；`display_data`：仅前端（当前未启用）
- VWD `[value, desc]` 二元组已废弃：路径直接指向值，如 `/环境/时间`

## 5. 更新模式与路由（双兼容布局）

- 同代更新：剧情模型一次生成正文 + `<UpdateVariable>` 块
- 额外更新模型通道：剧情模型生成正文，更新模型单独产出变量块（预设支持时启用）
- 条目命名约定（标记可出现在条目名任意位置，本布局统一前缀）：
  - 含 `[mvu_update]`：更新规则 / 输出格式 → 仅更新模型
  - 含 `[mvu_plot]`：剧情专属 CoT / 输出协议 → 仅剧情模型
  - 无标记：两模型共享（当前尽量不设，控制预算）
- 标记不绕过世界书常规激活 / 深度 / 粘性 / 顺序规则

## 6. 更新规则契约（模型可见约束）

- 输出块：`<UpdateVariable>`，含 `<analysis>` + `<JSONPatch>`，位于回复末尾且唯一
- op 白名单：`add` / `replace` / `remove` / `move`（禁止 delta / insert）
- `replace` 路径必须已存在；新增 record 项必须先 `add` 完整对象
- `<analysis>` 每行必须对应至少一条 patch
- 顶层容器白名单（当前）：环境、user、玉佩 —— 由 schema 强制 + 规则文本引导，变更需同步字段账本

## 7. 运行时依赖账本

| id | 角色 | 类 | 交付 | 回退 | 失效表现 |
|---|---|---|---|---|---|
| tavern-helper | 宿主扩展（全局 `z`、registerVariableSchema、eventOn、waitGlobalInitialized） | host_required | 需在宿主安装/启用 | — | 脚本全部失效 |
| mvu-loader | mvu_zod_cn.js（卡内封装） | embedded_required | 随卡封装 | 内置双镜像回退链 | 卡不可用 |
| zod-schema | zod_schema.js（卡内封装） | embedded_required | 随卡封装 | — | 校验失效 |
| mvu-bundle | MagVarUpdate bundle.js（解析/执行 JSONPatch） | remote_runtime | 运行时从 CDN 加载 | cdn ↔ testingcf 双镜像 | 变量不更新 |
| mvu-zod-helper | StageDog mvu_zod.js（registerMvuSchema 桥） | remote_runtime | 运行时从 CDN 加载 | cdn ↔ testingcf 双镜像 | schema 校验失效 |

用户须知（表述约定）：
- 已随卡封装，无需另行安装：加载器、schema、开局页（内联于 first_mes）
- 需要在宿主中安装或启用：JS-Slash-Runner（TavernHelper ≥ v4.3.7；开局页世界书开关需 ≥ v4.8）
- 运行时从远程地址加载：MagVarUpdate bundle、StageDog mvu_zod.js（含国内镜像回退）
- 仅开发环境需要：无

## 8. 验证门

离线（本阶段）：
- [x] zod_schema.js 与 InitialVariables.json 顶层容器/字段对齐
- [x] 字段账本每条字段有 owner 与生命周期
- [x] JSON / JS 语法检查通过
- [x] 模型可见文本无开发注释

真机（写卡阶段，未执行）：
- 新对话 / 重载 / 问候分支
- `<UpdateVariable>` 被应用、schema 拦非法结构、事件挂点刷新
- 双兼容标记在当前运行时的实际路由行为
- 开局页：条目开关持久生效、开场白切换首消息（swipe 机制）、无 TH 部分降级、prompt-only 正则（清单见 source/ui/开局页使用说明.md §7）

## 9. 假设与悬案

- bundle.js 行为基于 B1 指南（v1.0.0，SillyTavern v1.14.0 + JS-Slash-Runner v4.8.9 回源）——版本敏感，真机验收时复核
- `mag_variable_initiailized`（typo，多一个 i）未真机复核；卡内一律用 `Mvu.events.VARIABLE_INITIALIZED` 常量，禁止手写字符串
- 初始变量世界书条目的识别标记待真机/源码核实（写卡阶段做）
- 加载器注入默认设置的 API 以真机为准（当前为守卫式尝试，不阻塞加载）

## 10. 下一步

1. 用户确认框架与开局页
2. 写卡阶段：守护灵条目正文 → 四家条目正文 → 开局问候组装（first_mes = 开局页 + 叙事）→ 真机验收 → 打包
